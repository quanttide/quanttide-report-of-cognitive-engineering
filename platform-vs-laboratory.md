# Platform vs Laboratory

## 两种设计哲学

`apps/qtcloud-think`（量潮思考云）和 `examples/project-11`（情境引擎）共享同一个认知工程领域，但设计哲学完全不同。平台是**动词驱动**的操作管线，实验室是**名词驱动**的数据分层。前者告诉你"怎么加工"，后者告诉你"加工成什么"。

## 心智模型不在同一层

维度 | qtcloud-think（平台） | project-11（实验室）
|----|--------------|------------|
心智模型 | CODE：Clarify → Organize → Distill → Express | 情境 → 意图 → 图式
本质 | 处理动作的管线（动词） | 认知深度的层级（名词）
作用对象 | 单条原始想法 | 整个领域集合
产出 | 分类笔记（received/pending/rejected） | 周报 + 关系 + 图式
元层 | Meta agent | 元领域 + tension/coverage

它们不是同一心智模型的两个实现，而是**互补的维度**——平台定义了"怎么处理"，实验室定义了"处理成什么"。把加工流程和数据结构放在一起，才是完整的认知工程方法论。

## 三个独立得出的共同结论

尽管设计哲学不同，两个系统各自独立得出了三个相同的认知工程原则：

### 1. 必须有元层

平台设计 Meta agent 来反思自己的思维模式，实验室把"元"作为 16 个领域之一并用 `tension`/`coverage` 命令检视分析质量。两个系统在不同层面得出同一结论：认知工程工具无法绕开自我检视——谁分析，谁就要能被分析。

### 2. 三层处理架构自然涌现

| 层级 | 平台（设计层） | 实验室（实现层） |
|------|--------------|----------------|
| L1 | 本地规则 | 关键词匹配 + 意图层级规则 |
| L2 | 本地小模型 | 实体共享 / 框架重叠计算 |
| L3 | 云端 LLM | LLM 合成报告 |

平台是主动设计的架构，实验室是被动生长的实现，但分层完全一致。说明认知工程的工具在处理从简单到复杂的推理时，三层架构是自然收敛解。

### 3. 认知产品需要审核状态

平台对每条笔记标记 received / pending / rejected，实验室的意图数据也有类似的确认机制。认知工程加工的不是客观数据，而是经过判断的认知产品——"暂不接受为事实"是认知工程的合法状态，不是 bug。

## 断层：两个系统之间缺什么

平台的终点（笔记）到实验室的起点（gallery YAML）之间缺少自动映射：

- **CODE 的 Distill 技能是 TODO**。它正是设计用来完成这个断层的——把澄清后的想法精炼为结构化数据。
- **笔记 frontmatter 缺少认知深度层次**。当前只记录 summary/tags/original，没有 agenda/ecology/frame 的情境维度，也没有 agent/level/trigger 的意图维度。
- **审核状态在两个系统中各自独立定义**。笔记的 received 不等于 gallery 的"已审核通过"，缺少跨系统的一致状态协议。

## 不是上下游，是 X 轴和 Y 轴

最准确的比喻不是"平台是上游，实验室是下游"，而是平台定义的是 X 轴（处理步骤），实验室定义的是 Y 轴（数据深度）。平台 Clarify 一条想法之后，得到的数据可以在三个深度上同时有效——浅层的情境描述、中层的意图识别、深层的图式模式。实验室的分析也适用于平台正在处理的任何一条想法。

两者不是管道的上下游关系，而是一个二维空间的两个正交维度。哪一步处理对应哪个深度，是认知工程方法论的下一个待定义问题。
